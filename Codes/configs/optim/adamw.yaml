# Classic AdamW baseline (matches the original train_stage*.py scripts)
type: adamw
lr: 1.0e-4
betas: [0.9, 0.999]
eps: 1.0e-8
weight_decay: 0.0

# ExponentialLR(gamma=0.97) per epoch, as in the original code; set null to disable.
scheduler:
  type: exponential
  gamma: 0.97
